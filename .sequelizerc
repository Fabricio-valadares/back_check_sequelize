const path = require("path")

module.exports = {
    config: path.resolve(__dirname, "src", "shared", "config", "index.js"),
    'migrations-path': path.resolve(__dirname,  "src", "shared", "database", "migrations")
}